/**
 * file loader test spec
 */
'use strict';

import fileLoader = require('../src/utils/fileLoader');
import should = require('should');

describe('file loader test', () => {
  it('file loader is function', () => {
  });
});